require "spec_helper"

describe Paperclip::Eitheror do
  it "has a version number" do
    expect(Paperclip::Eitheror::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
